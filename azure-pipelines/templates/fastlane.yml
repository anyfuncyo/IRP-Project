parameters:
  - name: "lane"
    type: string

steps:
  - task: Bash@3
    displayName: "${{ parameters.lane }}"
    condition: succeededOrFailed()
    inputs:
      targetType: inline
      script: |
        bundle exec fastlane ${{ parameters.lane }}
